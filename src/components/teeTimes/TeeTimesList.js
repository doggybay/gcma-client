import React from 'react'
import { List } from 'semantic-ui-css'

import { TeeDate } from "./TeeDate";
import { TeeTime } from "./TeeTime";

export const TeeTimesList = ({ time }) => {
  console.log(time)
  //let timeToFormat = new Date(props.teeTime.time);

  return (
    <div></div>
  );
}

//export default TeeTimesList